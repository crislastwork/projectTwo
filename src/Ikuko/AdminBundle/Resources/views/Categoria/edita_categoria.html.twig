{% extends '::backend.html.twig' %}

{% block body %}

{% for flashMessage in app.session.flashbag.get('notice') %}
    <div class="flash-notice">
        {{ flashMessage }}
    </div>
{% endfor %}

<form action="{{ path('ikuko_admin_edita_categoria', {'categoria': categoria.id } ) }}" method="post"
        {{ form_enctype(form) }}>

    <div>
                {{ form_label(form.titolCa) }}
                {{ form_errors(form.titolCa) }}
                {{ form_widget(form.titolCa) }}
    </div>

    <div>
                {{ form_label(form.titolEs) }}
                {{ form_errors(form.titolEs) }}
                {{ form_widget(form.titolEs) }}
    </div>

            {{ form_widget(form) }}

    <input class="boton" type="submit" value="Desa els Canvis" />
</form> 

{% endblock %}
